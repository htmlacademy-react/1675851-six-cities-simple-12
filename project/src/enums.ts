export enum AppRoute {
  Root = '/',
  Offer = '/offer',
  Login = '/login'
}

export enum RatingType {
  Terribly = 'terribly',
  Badly = 'badly',
  NotBad = 'not bad',
  Good = 'good',
  Perfect = 'perfect'
}
